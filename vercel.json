{
  "buildCommand": "cd client && ng version && ng build --configuration \"$VERCEL_ENV\"",
  "framework": "angular",
  "installCommand": "npm update --global npm && npm ci && cd client && npm ci",
  "outputDirectory": "client/dist/mean-authentication/browser",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "x-robots-tag",
          "value": "none"
        }
      ]
    }
  ]
}
